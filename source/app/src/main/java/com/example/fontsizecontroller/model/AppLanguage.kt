package com.example.fontsizecontroller.model

/**
 * Ngôn ngữ hỗ trợ trong ứng dụng FontMaster.
 */
enum class AppLanguage(val code: String, val displayName: String) {
    VI("vi", "Tiếng Việt"),
    EN("en", "English")
}
